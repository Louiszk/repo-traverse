[tool.ruff]
line-length = 120

[tool.pyright]
typeCheckingMode = "standard"
venvPath = "."
venv = "repovenv"
extraPaths = ["backend"]

[tool.pytest.ini_options]
filterwarnings = [
    "ignore:.*httpx2.*",
]
